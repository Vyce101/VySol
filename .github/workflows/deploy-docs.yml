name: Deploy documentation

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - ".github/workflows/deploy-docs.yml"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  deploy:
    name: Build and deploy Retype
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Prepare Retype assets
        run: |
          mkdir -p docs/retype/static
          cp "docs/images/Logo 1.png" docs/retype/static/logo.png
          cp "docs/images/Transparent Hero 1.png" docs/retype/static/hero.png

      - name: Build Retype documentation
        id: build
        uses: retypeapp/action-build@latest
        with:
          config_path: docs/retype/retype.yml
          key: ${{ secrets.RETYPE_KEY }}

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ${{ steps.build.outputs.retype-output-path }}

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
